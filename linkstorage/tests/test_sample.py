"""
Пример модульного теста для проверки базовой работоспособности тестовой среды.

ВНИМАНИЕ: Этот файл предназначен только для проверки запуска pytest и логирования. Основные тесты приложения находятся в других файлах.
"""
import pytest
from app.utils.mylogger import Logger, ensure_log_directory

# Гарантируем наличие директории для логов и инициализируем логгер
ensure_log_directory()
logger = Logger("tests.test_sample", "logs/app.log")

def test_sample():
    """
    Пример простого теста, который всегда проходит.
    Используется для проверки запуска тестовой среды.
    """
    # Логируем начало теста
    logger.info("Выполняется тест test_sample")
    # Проверяем, что 1 равно 1 (тест всегда проходит)
    assert 1 == 1
    # Логируем успешное завершение теста
    logger.info("Тест test_sample успешно завершён") 